# project.faf — demo identity source
# IANA media type: application/vnd.faf+yaml
#
# Hosts can equally derive the same fields from .git, environment
# variables (e.g. GITHUB_REPOSITORY), or workspace configuration.
# The reference implementation only consumes the `repository` field.

repository: github.com/octocat/Hello-World
